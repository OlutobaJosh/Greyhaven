# ─── Server ───────────────────────────────────────────
PORT=3000
NODE_ENV=development

# ─── JWT Secret (change this to a long random string) ─
JWT_SECRET=change_this_to_a_very_long_random_secret_string_123

# ─── Admin Credentials ────────────────────────────────
ADMIN_EMAIL=admin@GreyHaven.com
ADMIN_PASSWORD=ChangeMe123!

# ─── Email (SMTP) ─────────────────────────────────────
# For Gmail: use App Password (not your real password)
# Google: myaccount.google.com > Security > App Passwords
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=2525
SMTP_SECURE=false
SMTP_USER=4d6c28c8a53cc4
SMTP_PASS=3fd87ecf96efde
EMAIL_FROM=GreyHaven Residential <no-reply@GreyHaven.com>

# ─── App URL (used in email links) ────────────────────
APP_URL=http://localhost:3000

